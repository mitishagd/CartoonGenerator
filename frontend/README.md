# Cartoon Generator Frontend
 
- npm create vite@latest . -- --template react-ts
- npm install styled-components
- npm install --save-dev @types/styled-components

touch .env.local
```

Open `.env.local` and add:
```
VITE_API_URL=api-url-here